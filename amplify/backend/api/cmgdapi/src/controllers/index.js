const project = require("./project");
const sample = require("./sample");
const check = require("./check");
const production = require("./production")

module.exports = { project, sample, check, production };
